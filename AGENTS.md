# AGENTS.md (plna)

이 저장소에서 작업하는 에이전트의 기본 정신과 실행 원칙.

## 0) 기본 정신

- 구현 속도보다 **요구사항 적합성**을 우선한다.
- "될 것 같다"가 아니라 **제약 조건 확인 후 계획**한다.
- 실패 원인은 기록하고, 같은 실수를 반복하지 않는다.

## 1) 계획 수립 전 필수 점검 (MANDATORY)

기능 개발 시작 전에 아래를 반드시 확인한다.

1. **인증/권한 제약**
   - API 키, OAuth, 스코프, 플랜 제한 여부
2. **데이터 제약**
   - 실제로 조회 가능한 데이터 범위/필드/기간
3. **운영 제약**
   - 스케줄 실행 가능성(시간대, cron), 장애 시 폴백
4. **보안 제약**
   - 비밀키 노출 금지(.env.local만), 민감정보 처리 정책
5. **검증 제약**
   - 로컬 재현 가능 테스트가 있는지, 머지 후 main 반영 검증 가능한지

위 5개 중 하나라도 미확정이면, "구현"이 아니라 "검증/확정" 태스크를 먼저 수행한다.

## 2) 실행 게이트

다음 조건을 만족해야 "완료"라고 보고한다.

- [ ] 코드/문서 변경이 의도한 브랜치에 반영됨
- [ ] 테스트/빌드 통과
- [ ] PR 머지 후 **main 기준 파일 상태 재확인**
- [ ] 사용자 요구사항 체크리스트 100% 충족
- [ ] 한계/미해결 이슈를 명시적으로 보고

## 3) 실패 방지 규칙 (X likes digest 같은 외부 API 작업 필수)

- 외부 API 연동은 "기능 구현" 전에 최소 1회 실제 호출로 접근성 확인
- 예상이 아닌 실측 결과(응답 코드/필드)를 기준으로 설계
- 범위 옵션(오늘/최근N/기간)을 초기 설계에 포함
- 자동 실행(07:00) 기능은 수동 실행 성공 후에만 활성화

## 4) 보고 방식

- 완료 보고는 "무엇을 했다" + "무엇을 검증했다" + "무엇이 아직 미확정인지" 3단 구조로 작성한다.
- 사용자에게 혼동을 줄 수 있는 표현(예: 아마 될 것) 금지.
