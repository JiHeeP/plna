{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "위젯 창(main)과 원격 페이지에 기본 권한을 준다. 드래그 바가 창 이동을 요청할 수 있도록 start-dragging 을 명시한다. npm run dev 로 로컬 서버를 띄워 볼 때도 드래그가 되도록 localhost 를 함께 적는다.",
  "windows": ["main"],
  "local": true,
  "remote": {
    "urls": [
      "https://plna.vercel.app/*",
      "http://localhost:*/*",
      "http://127.0.0.1:*/*"
    ]
  },
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}
